{% extends 'base.html.twig' %}

{% block title %}Modifier mot de passe{% endblock %}

{% block content %}

    <h1>Mon mot de passe</h1>
    {% if notification %}

        <div class="alert alert-info">{{ notification }}</div>

    {% endif %}
    {{ form(form) }}

    <a href="{{ path('app_account') }}">Retour au compte</a>
    <hr>


{% endblock %}
